vp staged
# Advisory React health scan of staged files; the full CI profile blocks regressions.
react-doctor --staged --blocking warning || {
  echo "React Doctor found staged regressions. Run 'npm run doctor' to inspect and fix." >&2
}
